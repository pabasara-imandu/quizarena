import type { MessageKey } from './en';

/**
 * Sinhala. Filled in from a native translation; any key missing here shows
 * its English text, so a partial dictionary is safe to ship.
 */
export const si: Partial<Record<MessageKey, string>> = {};
